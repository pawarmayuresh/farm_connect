pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "Pipeline from GitHub Working"
                sleep time: 2, unit: 'SECONDS'
            }
        }
    }
}
